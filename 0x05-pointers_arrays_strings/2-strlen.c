#include "main.h"
#include <string.h>

/**
 * _strlen - Count the number of characters
 * @s: pointer character parameter
 *
 * Description: Counts the number of character passed as input
 * Return: return type int
 */
int _strlen(char *s)
{
	int p;

	p = strlen(s);
	return (p);
}
