#include "main.h"
#include <stdio.h>

/**
 * _puts - Entry point
 * @str: pointer character parameter
 *
 * Description: Prints to stdout input from @str
 * Return: returns a string
 */
void _puts(char *str)
{
	 puts(str);
}
